# Render Blueprint for FindIt Backend
services:
  - type: web
    name: findit-backend
    runtime: python
    pythonVersion: "3.11.4"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
    plan: free
